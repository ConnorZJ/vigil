# AGENTS.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a repo-specific `AGENTS.md` that tells coding agents how to build, test, style, and maintain documentation in this repository.

**Architecture:** Gather commands and conventions from the existing Swift app, Bun plugin, and test layout, then capture them in a single top-level markdown file. Keep the file operational and concise, with explicit single-test commands and a hard requirement that future work follow `AGENTS.md` and update docs when product behavior changes.

**Tech Stack:** Markdown, Swift/AppKit, Bun, TypeScript, XCTest

---

### Task 1: Draft repository-specific guidance

**Files:**
- Create: `AGENTS.md`
- Reference: `README.md`
- Reference: `Makefile`
- Reference: `project.yml`
- Reference: `plugin/package.json`
- Reference: `Vigil/State/AppState.swift`
- Reference: `Vigil/Ghostty/GhosttyWindowActivator.swift`
- Reference: `plugin/src/plugin.ts`
- Reference: `plugin/test/transport.test.ts`

- [ ] **Step 1: Draft the file structure**

Include sections for commands, style rules, tests, and documentation upkeep.

- [ ] **Step 2: Encode exact commands**

Include build/test/typecheck commands, explicitly documenting when the repository has no separate lint command. Include exact single XCTest method and single Bun test invocations.

- [ ] **Step 3: Encode repo-specific code style guidance**

Cover Swift and TypeScript conventions that are visible in the current codebase, citing patterns from representative source and test files rather than generic advice.

### Task 2: Add maintenance requirements

**Files:**
- Modify: `AGENTS.md`

- [ ] **Step 1: Add implementation rule**

State that future work in this repository must follow `AGENTS.md`.

- [ ] **Step 2: Add documentation sync rule**

State that behavior, feature, command, and workflow changes must be reflected in docs.

- [ ] **Step 3: Note external rule files**

State that no `.cursorrules`, `.cursor/rules/`, or `.github/copilot-instructions.md` files currently exist in this repository.

### Task 3: Verify the document

**Files:**
- Test: `AGENTS.md`

- [ ] **Step 1: Read the final file**

Confirm it is repository-specific, around 150 lines, and includes the requested command coverage.

- [ ] **Step 2: Report completion**

Tell the user the file was created and that future implementation should follow it.
